
// 1. Марки комментарии и организация файла по порядку (наведение порядка по подобию других контроллеров)
// 2. При выборе дома избранным должна меняться модель
// 3. Просмотреть еще раз организацию моделей ячеек с видами материалов
// 4. Доделать актуальные цены при создании категорий типа houseConfiguration?.terraces 
// 4. 📝 Домашнее задание — Приложение «Каталог домов»

> Ты начинаешь строить приложение для просмотра каталога домов — как на сайте [villozi.com](https://villozi.com).
> Каждый урок ты будешь добавлять новую часть этого приложения.
> В этом уроке — только модели данных (Swift-структуры), без UI.

Создай новый Swift-файл HouseModels.swift.

### Задание 1 — Модель дома - done
Создай struct House с полями:
— уникальный номер проекта
— название, например "60-23 Виллози-дом"
— площадь в м²
— количество спален
— цена от (в рублях)
— ссылка на фото
— метка, например "Энергоэффективный"

### Задание 2 — Enum типа дома !!!!!
Создай enum HouseType с кейсами:
- compact — до 60 м²
- standard — 60–100 м²
- large — более 100 м²

Добавь rawValue с русскими названиями ("Компактный", "Стандарт", "Большой").

Добавь в House поле HouseType.

### Задание 3 — Массив и фильтрация - done
Создай массив let catalog: [House] из 4 домов (придумай данные сам).

Напиши функцию, которая возвращает только дома нужного типа.

### Задание 4 — Класс избранного
Создай class Favorites с:
- список избранных домов
- функцией добавить в избранное
- функцией убрать из избранного
- вычисляемое свойство, количество избранных
- функцией проверки есть ли дом в избранном (сравнивай по id)

### 💡 Подсказки
- enum HouseType: String — добавь rawValue для отображения названия типа
- items.first(where: { $0.id == house.id }) != nil — проверка наличия в массиве
- Вычисляемое свойство count: var count: Int { items.count }

0. Сохранять в юзердефолтс массив айдишников домов
1. Cоздать экран с таблицей домой избранное (можно переиспользовать ячейку)
2. Получать из ЮзерДефолтс массив айдишников домов
2. Данные на экране формировать из датасорс, но предварительно фильтровать по массиву айдишников из юзердефолтс 
3. Отображаешь только избранные дома
4. При нажатии на снятие сердечка из избранного, то удаляешь из юзердефолтс, из массива экрана и делаешь tableView.removeRow по индекспасу

// 0. make commit and push - force-push
// 1. Инициализировать датасорс здесь по подобию фейворитс
// 2. Во вьюдидлоад читаем избранные дома из юзердефолтс, обновляем изфейворит свойства у домов и сохраняем в датасорс
// 3. Добавить вьювиллаппеар, в котором ты будешь читать дома из датасорса и обновлять таблицу




// 5 april
// 0.0. Пройтись по тому что написано, чтобы немного осознать, а на следующем уроке будет опрос. CatalogOfHousesViewController

// 1. Создать тапжест и добавить на вью нажатие addTarget, разрешить вью обрабатывать нажатию (isUserInteractionEnabled)
// 2. В функции которая вызывается при нажатии открываем через презент новый экран
// 3. На новом экране по центру будет картинка, либо галерея без пейджконтрола. Вверху экрана кнопка закрыть, либо сделать навигейшн контроллер с кнопкой закрыть
// 4. На новый экран нужно передавать картинку, либо массив картиног для галереи,


// 6 april
// 0.0. Пройтись по тому что написано, чтобы немного осознать, а на следующем уроке будет опрос. CatalogOfHousesViewController

### Задание 1 — @escaping

Создай функцию `loadHousesWithDelay` которая:
- Принимает `completion: @escaping ([House]) -> Void`
- Через 1 секунду (используй `DispatchQueue.global().asyncAfter`) вызывает `completion` с массивом из 3 домов

```swift
func loadHousesWithDelay(completion: @escaping ([House]) -> Void) {
    // твой код
}
```

### Задание 2 — Уведомление об изменениях

Добавь в класс `Favorites` из урока 1 замыкание-уведомление:

```swift
var onUpdated: (() -> Void)?
```

Вызывай его в методах `add` и `remove`. Проверь в `main.swift` или Playground:

```swift
let favorites = Favorites()
favorites.onUpdated = {
    print("Избранное обновлено! Сейчас \(favorites.count) домов")
}
favorites.add(catalog[0])  // должно напечатать "Избранное обновлено! Сейчас 1 домов"
```

### Задание 3 — Цепочка операций

Напиши одно выражение (цепочку `.filter { }.sorted { }.map { }`) которое:
- Оставляет только дома с 2 и более спальнями
- Сортирует по цене (дешевле сначала)
- Возвращает массив строк вида `"Название — N спал."`
